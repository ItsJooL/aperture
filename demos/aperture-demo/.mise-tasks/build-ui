#!/usr/bin/env bash
#MISE description="Build the aperture-demo web UI"
set -euo pipefail

TASK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
DEMO_DIR="$(cd "$TASK_DIR/.." && pwd)"
UI_DIR="$DEMO_DIR/web-ui"

cd "$UI_DIR"
if [ ! -d node_modules ]; then
  npm install
fi
npm run build
