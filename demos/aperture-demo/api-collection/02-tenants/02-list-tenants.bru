meta {
  name: List Tenants
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/manage/tenants?page=0&size=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{superadminToken}}
}

assert {
  res.status: eq 200
  res.body.items: isDefined
}
