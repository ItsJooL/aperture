meta {
  name: List Users with Search
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/manage/tenants/{{tenantId}}/users?search=viewer&page=0&size=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{superadminToken}}
}

assert {
  res.status: eq 200
}
