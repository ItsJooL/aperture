meta {
  name: Disable Service Account
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/manage/tenants/{{tenantId}}/service-accounts/{{serviceAccountId}}/disable
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tenantAdminToken}}
}

assert {
  res.status: eq 200
}
