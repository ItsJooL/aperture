meta {
  name: Logout Superadmin
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/logout
  body: json
  auth: none
}

body:json {
  {
    "refreshToken": "{{superadminRefreshToken}}"
  }
}

assert {
  res.status: eq 204
}
