apiVersion: aperture.itsjool.com/v1
kind: RoleDefinition
metadata:
  name: roles
spec:
  roles:
    Admin:
      description: "Full access — maps to Keycloak realm role Admin"
    User:
      description: "Read and create orders — maps to Keycloak realm role User"
