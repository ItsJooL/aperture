meta {
  name: Get User Token
  type: http
  seq: 2
}

post {
  url: {{keycloakUrl}}/realms/{{keycloakRealm}}/protocol/openid-connect/token
  body: formUrlEncoded
  auth: none
}

body:formUrlEncoded {
  grant_type: password
  client_id: {{keycloakClientId}}
  username: {{userUsername}}
  password: {{userPassword}}
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("userToken", res.body.access_token);
  }
}

assert {
  res.status: eq 200
  res.body.access_token: isDefined
}
