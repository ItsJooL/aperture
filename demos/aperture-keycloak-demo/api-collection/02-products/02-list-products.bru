meta {
  name: List Products
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/products
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

headers {
  Accept: application/vnd.api+json
}

assert {
  res.status: eq 200
  res.body.data: isDefined
}
