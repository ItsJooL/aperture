meta {
  name: List Tasks
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/tasks
  body: none
  auth: bearer
}

auth:bearer {
  token: {{superadminToken}}
}

headers {
  Accept: application/vnd.api+json
}

assert {
  res.status: eq 200
  res.body.data: isDefined
}
