apiVersion: aperture.itsjool.com/v1
kind: FrameworkConfig
metadata:
  name: config
spec:
  tenancyMode: none
  defaultRoles:
    - Admin
    - Assistant
    - ReadOnly
  mcp:
    enabled: true
    transport: stateless
