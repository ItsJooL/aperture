meta {
  name: List Notes
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/notes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/vnd.api+json
}

assert {
  res.status: eq 200
  res.body.data: isDefined
}
